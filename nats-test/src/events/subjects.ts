export enum Subjects {
  TicketCreated = 'ticket.created',
  OrderUpdated = 'order.updated',
}

export enum Streams {
  Tickets = 'tickets', // ticket.created, ticket.updated...
  // Orders = 'orders', // order.updated...
}
